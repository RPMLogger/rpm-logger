// ─── TABS / FINANCIAL.JS ─────────────────────────────────────────────────────
// The Financial tab is now just two links to the Google Sheets (see index.html).
// No data fetching or rendering happens here anymore. This stub exists only so the
// switchTab('financial') call has something to invoke.
function initFinancialTab() {}
